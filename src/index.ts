import { InstagramClientInterface } from "./client";

const instagramPlugin = {
    name: "instagram",
    description: "Instagram client plugin",
    clients: [InstagramClientInterface],
};

export default instagramPlugin;